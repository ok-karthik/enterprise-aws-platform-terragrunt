name: 'Terragrunt Plan Job'

# Reusable workflow to handle matrix planning and high-precision governance
on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      module_list:
        required: true
        type: string
      aws_region:
        required: true
        type: string
      aws_role_arn:
        required: true
        type: string

permissions:
  contents: read
  id-token: write

jobs:
  # ─────────────────────────────────────────────────────────────
  # PHASE 1: Infrastructure Planning
  # ─────────────────────────────────────────────────────────────
  plan:
    name: '🚀 Plan: ${{ matrix.module.name }}'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module: ${{ fromJson(inputs.module_list) }}
    env:
      AWS_REGION: ${{ inputs.aws_region }}
      AWS_ROLE_ARN: ${{ inputs.aws_role_arn }}
      TG_LOG_CUSTOM_FORMAT: "%msg"
    
    steps:
      - name: Git Checkout
        uses: actions/checkout@v4

      - name: Setup Platform
        uses: ./.github/actions/setup-platform
        with:
          aws_region: ${{ env.AWS_REGION }}
          aws_role_arn: ${{ env.AWS_ROLE_ARN }}

      - name: Terragrunt Init & Plan
        id: plan
        run: |
          echo "PENDING" > summary.txt
          if ! terragrunt init --non-interactive; then
            echo "INIT FAILED" > summary.txt
            exit 1
          fi
          
          PLAN_FILE="$(pwd)/tfplan.bin"
          if ! terragrunt plan --non-interactive -no-color -out="$PLAN_FILE" 2>&1 | tee /v/stderr | sed 's/^.*terraform: //g' > plan_raw.txt; then
            echo "PLAN FAILED" > summary.txt
            exit 1
          fi
          
          terragrunt show -json "$PLAN_FILE" > tfplan.json
          tf-summarize tfplan.json > summary.txt || echo "SUMMARIZE FAILED" > summary.txt
        working-directory: ${{ matrix.module.path }}
        continue-on-error: true

      - name: Bundle Artifacts
        if: always()
        run: |
          mkdir -p dist
          cp ${{ matrix.module.path }}/summary.txt dist/ || true
          cp ${{ matrix.module.path }}/plan_raw.txt dist/ || true
          cp ${{ matrix.module.path }}/tfplan.json dist/ || true
          echo "${{ matrix.module.path }}" > dist/metadata.txt
          
          SLUG=$(echo "${{ matrix.module.path }}" | sed 's/\//-/g')
          echo "SLUG=$SLUG" >> $GITHUB_ENV

      - name: Upload Plan Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-${{ env.SLUG }}
          path: dist/
          retention-days: 1

  # ─────────────────────────────────────────────────────────────
  # PHASE 2: Governance Audit (Policy Checks)
  # ─────────────────────────────────────────────────────────────
  audit:
    name: '🛡️ Policy check: ${{ matrix.module.name }}'
    needs: [plan]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module: ${{ fromJson(inputs.module_list) }}
    steps:
      - name: Git Checkout
        uses: actions/checkout@v4

      - name: Setup Platform
        uses: ./.github/actions/setup-platform

      - name: Prepare Module Slug
        run: |
          SLUG=$(echo "${{ matrix.module.path }}" | sed 's/\//-/g')
          echo "SLUG=$SLUG" >> $GITHUB_ENV

      - name: Download Plan JSON
        uses: actions/download-artifact@v4
        with:
          name: plan-${{ env.SLUG }}
          path: plan_data

      - name: ⚖️ Policy Gate (OPA)
        run: |
          echo "Running Governance checks on ${{ matrix.module.name }}..."
          conftest test plan_data/tfplan.json --policy $GITHUB_WORKSPACE/policies/terraform

      - name: 🛡️ Security Gate (Checkov Plan)
        uses: bridgecrewio/checkov-action@v12
        with:
          file: plan_data/tfplan.json
          config_file: .checkov.yaml
          soft_fail: true
